name: UI Pipeline

on:
  push:
    braches: [ "main" ]
    
jobs:
  build:
    runs-on: self-hosted

    steps:
      - name: Navigate to build directory
        run: cd ../
        
      - name: Show files
        run: pwd
