name: UI Pipeline

on:
  push:
    braches: [ "main" ]
    
jobs:
  build:
    runs-on: self-hosted

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        
      - name: Got to the build directory
        run: cd ./pomoticker/ui

      - name: Verify directory
        run: pwd
