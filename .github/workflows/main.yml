name: UI Pipeline

jobs:
  build:
    runs-on: self-hosted

    steps:
      - name: Test
        run: echo "Testing pipeilne execution"
      - name: Test folder creation
        run: mkdir folder_from_pipeline
